import loading from "../assets/loading.png";

const Loading = () => {
  return (
    <div>
      <img src={loading} alt="Loading" width={"300px"} height={"300px"} />
    </div>
  );
};

export default Loading;
